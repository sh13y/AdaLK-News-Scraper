name: Scrape Latest News

on:
  schedule:
    - cron: "0 * * * *"  # Every hour at minute 0
  workflow_dispatch:  # Allows for manual trigger

jobs:
  scrape_and_update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Scraping Script
        run: python scrape_news.py  # Ensure the script name is correct

      - name: Commit and Push Changes
        run: |
          git config --global user.name "${{ secrets.GIT_USER_NAME }}"
          git config --global user.email "${{ secrets.GIT_USER_EMAIL }}"
          echo "Git configured with user.name=${{ secrets.GIT_USER_NAME }} and user.email=${{ secrets.GIT_USER_EMAIL }}"
          git add README.md
          git add README.md news_log.json  # Add both files
          git commit -m "Update README.md and news_log.json with latest news"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
